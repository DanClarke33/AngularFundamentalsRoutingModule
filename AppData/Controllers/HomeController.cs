namespace AppData.Controllers
{
    using System.Web.Mvc;

    public class HomeController : Controller
    {
        // NOTE: If we were using .Net for the delivery of the site instead of just html pages in a folder we would use this to return our index page
        /*public ActionResult Index()
        {
            return View();
        }*/
    }
}
